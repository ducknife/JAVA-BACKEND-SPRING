spring:
    application:
        name: project
    profiles:
        active: dev

admin:
    bootstrap:
        fullname: ${ADMIN_FULLNAME:Admin}
        username: ${ADMIN_USERNAME:admin}
        password: ${ADMIN_PASSWORD:admin}
        email: ${ADMIN_EMAIL:admin@alandas.site}

jwt: 
    secret-key: ${JWT_SECRET_KEY:051ISFsCVKV9chsyd7r2eU2MC60HfCHish/m05hL86rNYom38Nw7xmocIPTkOCVOYIVcaEBeU8osmZO++LpCMA==}

# ```
# Ưu tiên từ CAO → THẤP:
# ──────────────────────
# 1. Command line arguments (--server.port=9090)
# 2. Environment variables (SERVER_PORT=9090)
# 3. application-{profile}.properties
# 4. application.properties
# 5. Default values trong code
# ```